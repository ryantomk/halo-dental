// TODO: Implement RoleGuard.
// Client-side role check (UI hiding only) — server-side route guard in admin layouts is the real boundary.
// See halo-dental-technical-specification.md, Section 5 (Component structure).

export default function RoleGuard() {
  return null;
}
